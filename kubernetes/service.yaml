apiVersion: v1
kind: Service
metadata:
  name: website
  labels:
    app.kubernetes.io/name: website
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: website
  ports:
    - name: http
      appProtocol: http
      port: 80
      targetPort: http
